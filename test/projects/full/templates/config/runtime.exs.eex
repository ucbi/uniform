# 1 + 1 = <%= 1 + 1 %>
# App name is <%= app.name.underscore %>

<%= if depends_on?(app, :mix, :esbuild) do %>
  # Depends on esbuild
<% end %>

<%= if depends_on?(app, :mix, :lhttpc) do %>
  # Depends on lhttpc
<% end %>

# <%= inline_upcase("Inline upcase") %>
# <%= upcase("String.upcase") %>
